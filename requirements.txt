# 앱 실행에 필요한 의존성.
# 하한 1.50 - 버튼/컨테이너의 width="stretch" API가 이 버전부터 제공된다.
# 상한 2.0  - 메이저 업그레이드로 API가 바뀌는 것을 막는다.
streamlit>=1.50,<2.0

# 테스트 실행용.
pytest>=8.0,<10.0

# AI 셰프(선택 기능)용. API 키가 없으면 호출되지 않으며, 앱의 나머지 기능은 그대로 동작한다.
# Responses API의 구조화 출력(json_schema)을 사용하므로 3.x가 필요하다.
openai>=3.0,<4.0
python-dotenv>=1.0,<2.0
